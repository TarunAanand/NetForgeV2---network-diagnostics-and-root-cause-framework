---
kind: external_dependency
name: Rich Terminal UI Library
slug: rich
category: external_dependency
category_hints:
    - framework_behavior
scope:
    - '**'
---

Used for formatted terminal output (tables, colored status indicators) across the CLI and diagnosis formatter. No custom Rich extensions are defined — only the public table/rule/theme APIs are consumed.